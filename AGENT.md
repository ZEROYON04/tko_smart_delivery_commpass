## プロジェクト背景

本アプリは「スマート配送コンパス in 広島」と呼ばれる、物流2024年問題と人口減少を解決するためのプラットフォームです。
受取人を責めるのではなく、「急な予定変更は起こるもの」という前提に立った、受取人・ドライバー協調型の動的ステータス変更システムを構築します。

## 技術要件・制約

- **パッケージマネージャー**: 必ず `pnpm` (v11.9.0以上) を使用してください。`npm`や`yarn`は `only-allow pnpm` により制限されています。
- **Node.js バージョン**: v26.4.0以上が必須です。
- **フレームワーク**: Next.js 16.2.12 (App Router) および React 19.2.4。
- **BaaS (Supabase)**: `@supabase/ssr` を用いたサーバーサイドレンダリング(SSR)対応の認証およびデータ取得を実装してください。
- **スタイリング**: Tailwind CSS v4 を使用します。
- **コード品質管理**:
- ESLint (v9系 Flat Config) および Prettier が設定されています。
- SQLフォーマット用の `prettier-plugin-sql` も導入されています。
- `husky` と `lint-staged` による pre-commit フックが有効なため、品質基準を満たさないコードのコミットは弾かれます。

## 実装における方針

- **ユーザー導入障壁の排除**: 受取人側は専用アプリを要求せず、LINE連携（公式アカウント）のみで完結する軽量でストレスフリーな設計とします。
